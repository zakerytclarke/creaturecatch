{
  "name": "creaturecatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Creature Catch — a fully custom, frontend-only creature-collecting RPG.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
